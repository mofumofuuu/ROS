# 定义srv文件
```
int64 a   //请求数据
int64 b
---    
int64 sum  // 发送数据 
2 在package.xml 添加依赖
与话题类似
3 在cmakeLists里面去修改添加编译选项
与话题类似 
多了这一部分
add_service_files(
   FILES
   AddTwolnts.srv
#   Service2.srv
 )
``` 
4 编写server和client去实现服务，并在cmakelists里面添加可执行文件

```
add_executable(server src/server.cpp)
target_link_libraries(server ${catkin_LIBRARIES})
add_dependencies(server ${PROJECT_NAME}_gencpp)
add_dependencies(client src/client.cpp)
target_link_libraries(client $(catkin_LIBRARIES))
add_dependencies(client ${PROJECT_NAME}_gencpp)
```